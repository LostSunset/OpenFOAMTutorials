#!/bin/bash
#
#----------------------------------------------#
# Contributor: Tobias Holzmann                 #
# Updated on:  22 April 2020                   #
#----------------------------------------------#
# Location:    https://Holzmann-cfd.com        #
# Email:       community@Holzmann-cfd.com      #
#----------------------------------------------#


#------------------------------------------------------------------------------
# Preamble with the information about the tutorial
echo -en "
-------------------------------------------------------------------------------
${GRAY}Tutorial by Tobias Holzmann${NC} :: Generated on April 2020
${GRAY}Build for OpenFOAM version${NC}  :: OpenFOAM-7.x
${GRAY}Tested on system${NC}            :: Ubuntu 19.10
${GRAY}The case was updated on${NC}     :: 22 April 2020


${GREEN}Topic: Re-build of the first cfd analysis of Tobias Holzmann${NC}
-------------------------------------------------------------------------------
Note: The investigation is just a demonstration and does not provide enough
data to fulfill the reality. Furthermore, Tobias did not investigate into that
case. It was just the first analysis he saw from OpenFOAM.\n
"


#------------------------------------------------------------------------------
# Check the OpenFOAM version
echo -e "   - Check OpenFOAM version. We need $foamVersion"
userFoamVersion=`echo "$WM_PROJECT-$WM_PROJECT_VERSION"`

if [ "$userFoamVersion" == "$foamVersion" ]
then
    echo -e  "   - ${GREEN}$foamVersion found...${NC}"

else
    echo -e  "   - ${ORANGE}You are not using the correct OpenFOAM version"
    echo -en "     Your version is $userFoamVersion and we need ${GREEN}"
    echo -e  "$foamVersion${NC}\n"

    while true
    do
        echo -en  "   - Proceed with your OpenFOAM version (y/n): "
        read proceed

        if [ "$proceed" == "y" ]
        then
            break

        elif [ "$proceed" == "n" ]
        then
            echo -e "\n\n"
            exit

        fi
    done
fi


#------------------------------------------------------------------------------
